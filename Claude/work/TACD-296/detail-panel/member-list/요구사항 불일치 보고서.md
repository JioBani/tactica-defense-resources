# TACD-296 요구사항 불일치 보고서

## 개요

- 구현 단위: `member-list` (SR 그룹 C — 시너지 보유 소환수 목록 표시)
- 작성 시점: 2026-05-11
- 불일치 건수 (본 구현 단위 DoD 범위): 0
- 회귀 의심 MIS 건수 (본 단위 범위 외): 5 — 사용자 결정으로 *본 사이클 비책임 baseline 회귀* 로 분류 (effect-info 사이클과 동일 5건).
- 전체 통과 (본 단위 범위): Y
- 테스트 실행 명령: UnityMCP `run_tests` — mode=EditMode, assembly=`Assembly-CSharp-Editor`
- 전체 실행 결과: total=303, failed=5 (모두 baseline 회귀 5건과 동일), 본 단위 `SynergyDetailMemberListTests` 15 케이스는 failures 목록에 없음 → 통과.

## 테스트 어셈블리 / 코드 위치

- 테스트 파일: `Assets/Tests/Editor/SynergyDetailMemberListTests.cs`
- 어셈블리: 묵시적 `Assembly-CSharp-Editor`.
- 네임스페이스: `Tests.Editor`.
- 테스트 케이스 명세: `Claude/work/TACD-296/detail-panel/member-list/테스트케이스명세.md`

## 본 단위 DoD 검증 결과

| DoD ID | 검증한 TC ID | 결과 |
|---|---|---|
| DoD-S11 | TC-1201, TC-1202, TC-1203 | Pass |
| DoD-S12 | TC-1301, TC-1302 | Pass |
| DoD-S13 | TC-1401, TC-1402, TC-1403, TC-1404, TC-1405 | Pass |
| DoD-S14 | TC-1501, TC-1502, TC-1503 | Pass |
| 라이프사이클 보강 | TC-1601, TC-1602, TC-1603 | Pass |

본 구현 단위(SR 그룹 C) DoD-S11 ~ DoD-S14 자체는 모두 통과.

## 불일치 항목

### 본 구현 단위 범위 내

(없음.)

### 회귀 의심 — 본 단위 범위 외 (지속)

직전 effect-info 사이클의 보고서에 기록된 5건 (MIS-001 ~ MIS-005) 이 본 사이클의 전체 실행에서도 동일하게 실패. 사용자 결정으로 *본 사이클 비책임 baseline 회귀* 로 분류된 상태이며 본 사이클 작업이 새로 도입한 회귀 아님 (effect-info 사이클 종료 시점 분류 결과 그대로 유지).

| 회귀 의심 테스트 (변동 없음) | 기대 / 실제 |
|---|---|
| ArcanistSynergyControllerTests.PlacementChanged_BattleArea_AppliesSpellPowerToNonSynergyDefender | 25.0 / 10.0 |
| ArcanistSynergyControllerTests.PlacementChanged_BattleArea_AppliesSSEToSynergyDefender | 35.0 / 10.0 |
| SynergyControllerTests.DefenderLeavesAndReenters_SSEReapplied | 125.0 / 100.0 |
| SynergyControllerTests.NewDefenderEntersWhileActive_SSEApplied | 125.0 / 100.0 |
| SynergyControllerTests.TwoDefendersEnter_TierActivates_SSEApplied | 125.0 / 100.0 |

**분류 (effect-info 사이클에서 결정된 분류 그대로 적용)**: 본 사이클 비책임 baseline 회귀.
**분류 사유**: member-list 사이클의 변경 영역 (`SynergyDetailMemberList` 본체 + `SynergyManager.SynergyMembers` 외부 노출 + UXML/USS 추가 + `SynergyDetailPanel.cs` 의 DefenderManager 직렬화 보강 1라인) 어느 것도 SSE 적용 경로의 동작을 바꾸지 않음 — `SynergyMembers` 인덱스 신규 노출은 UI 측 외부 조회 통로일 뿐 SSE 적용 책임에 관여하지 않으며, `SynergyManager.HandleDefenderChanged` (SSE 주입 라인) 도 본 사이클에서 미수정. 따라서 본 사이클은 이 회귀에 비책임으로 분류된다.

## 미커버 항목 (보고용)

- 명세 §미커버 / 보류 항목 참조. 본 단위 DoD 범위 자체에서는 별도 위임 없음.
- *DefenderManager 의 prefab wiring* — 사용자 후속 작업으로 분리됨 (impl 메모 §prefab wiring 안내). 본 단위 코드 책임은 *DefenderManager 가 주입된 상태에서 올바른 활성 / 비활성 판정* 으로 정의되며, 그 책임 자체는 본 보고서의 TC-1401 ~ 1405 로 충족 검증됨.

## 해결 기록

- 최초 실행: 2026-05-11 — 본 단위 DoD 0 건 불일치, 회귀 의심 5 건 발견 (baseline 회귀 — 이전 사이클 분류 그대로 유지).
