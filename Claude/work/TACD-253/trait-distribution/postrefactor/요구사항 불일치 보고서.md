# TACD-253 SummonTrait 통합 (post-refactor) 요구사항 불일치 보고서

## 개요

- 통합 구현 단위 (post-refactor) — 이전 trait-store / trait-distribution / trait-synergy-integration 3 사이클이 SynergyManager 단일 클래스로 응집된 이후의 단일 검증.
- 대응 SR DoD: DoD-S1 ~ DoD-S7 전부.
- 대상 변경:
  - 수정 `Assets/Scenes/Battle/Feature/Synergy/Scripts/SynergyManager.cs` (`summonTraitMap` · `summonTraitRegistry` · `_summonTraitService` · `DistributeAndIntegrateSummonTraits` · `GetSummonTrait` · `OnBattleWin/Lose` 핸들러 통합)
  - 수정 `Assets/Scenes/Battle/Feature/Round/RoundManager.cs` (`OnBattleStartEventDto` publish 1 줄 폐기)
  - 폐기: `SummonTraitStore` / `SummonTraitDistributor` / `OnBattleStartEventDto` / `OnSummonTraitsDistributedEventDto`
  - 유지: `SummonTraitService` (행위 동일) / `SummonTraitRegistry` / `NoOpSynergyStatusEffect` / `SummonTraitSynergyController` / `SynergyControllerFactory` 의 SummonTrait 분기
- 작성 시점: 2026-05-17 (리팩토링 사후 재작성)
- 불일치 건수: 0 개
- 전체 통과: Y
- 테스트 실행 명령: UnityMCP `run_tests` (mode=EditMode, group_names=`Tests.Editor.SummonTraitIntegrationTests`)
- 테스트 위치: `Assets/Tests/Editor/SummonTraitIntegrationTests.cs` (namespace `Tests.Editor`)
- TC 개수: 19 (TC-001~004, TC-101~103, TC-201~203, TC-301~305, TC-401, TC-501/502, TC-601)
- TC 결과: 19/19 통과 (durationSeconds 0.624)

## 불일치 항목

(없음 — 본 구현 단위 범위의 모든 테스트가 첫 실행에서 기대 동작과 일치)

## 사전 사이클과의 관계

본 보고서는 이전 3 사이클(trait-store / trait-distribution / trait-synergy-integration) 의 산출물을 대체하지 않고 *리팩토링 사후 재작성* 으로 병기된다. 이전 산출물은 각자 디렉토리에 그대로 보존되며, 그 시점의 분리 구조에 대한 검증 기록이다.

리팩토링 결정 문서:
- `Claude/refactoring/2026-05-17-TACD-253-synergy-init-symmetry.md` — 트리거 비대칭 해소 (이벤트 체인 → 직접 호출)
- `Claude/refactoring/2026-05-17-TACD-253-summontraitstore-removal.md` — Store/Distributor 분리 안티패턴 해소

본 사이클의 테스트는 새 구조 — SynergyManager 단일 클래스 통합 — 의 black-box 책임을 검증한다.

## 본 구현 단위 범위 외 회귀 / 사전 실패

본 단계 테스트는 본 클래스만 필터링하여 실행했다(group_names). 이전 사이클들과 동일한 baseline 사전 실패 영역(`ArcanistSynergyController*` / `SynergyController*` / `SynergyDetail*`) 은 본 구현 단위의 코드 경로와 직접 결합 없음. 별도 백로그.

## 해결 기록

- 최초 실행 (재작성 후): 2026-05-17 — 19 건 전체 통과 (durationSeconds 0.624). 첫 실행에서 모든 케이스 통과.
- 종료: 2026-05-17 — 본 구현 단위에 불일치 0 건, 전체 통과. 스테이지 7(불일치 해소) 무동작.

## 명세 메모

- **DoD-S1·S2·S3**: 통합 흐름 `Start → SummonTraitService.Distribute → summonTraitMap 채움` 을 TC-001 로 black-box 검증. Service 자체의 무효 입력 경계 케이스(이전 사이클의 8 케이스) 는 본 통합 사이클에 포함하지 않음 — 미커버 §보류 항목 참조.
- **DoD-S4**: `OnBattleWin/Lose` 핸들러의 `summonTraitMap.Clear()` 를 TC-201/202 로 검증. 라이프사이클(OnEnable/OnDisable) 구독·해제 보장은 TC-203 으로 결정 테이블 처리.
- **DoD-S5·S6**: UI 미변경 정책 black-box 충분 조건 — `SynergyActivations` (TC-002) · `SynergyMembers` (TC-003) 가 SummonerEffect + SummonTrait 합산형으로 채워지는 것이 표시·노출의 충분 조건.
- **DoD-S7**: 컨트롤러 생성(TC-004) + Defender Spawn 시 두 시너지 부여(TC-301~305 결정 테이블) + Factory routing(TC-501/502) + NoOpSSE 생성(TC-601) 으로 커버.
- **registry 가드(TC-401)**: 새 구조에서는 registry 가 SynergyManager 의 [SerializeField] 로 옮겨갔으므로, 가드 위치도 SynergyManager.DistributeAndIntegrateSummonTraits 의 진입부 — 이 경로를 직접 검증.
