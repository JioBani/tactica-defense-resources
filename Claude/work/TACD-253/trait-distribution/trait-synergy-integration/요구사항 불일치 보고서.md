# TACD-253 trait-synergy-integration 요구사항 불일치 보고서

## 개요

- 구현 단위: `trait-synergy-integration` (SR 그룹 C / DoD-S5·DoD-S6·DoD-S7)
- 대상 변경:
  - 신규 `Assets/Scenes/Battle/Feature/Events/RoundEvents/OnSummonTraitsDistributedEventDto.cs`
  - 신규 `Assets/Scenes/Battle/Feature/Synergy/Scripts/SynergyEffects/NoOpSynergyStatusEffect.cs`
  - 신규 `Assets/Scenes/Battle/Feature/Synergy/Scripts/SynergyControllers/SummonTraitSynergyController.cs`
  - 수정 `Assets/Scenes/Battle/Feature/SummonTrait/SummonTraitDistributor.cs` (Initialize 후 publish)
  - 수정 `Assets/Scenes/Battle/Feature/Synergy/Scripts/SynergyControllerFactory.cs` (SynergyType.SummonTrait 분기)
  - 수정 `Assets/Scenes/Battle/Feature/Synergy/Scripts/SynergyManager.cs` (`_unitSummonTraitMap` 신규, `HandleSummonTraitsDistributed` 신규, Start 종료부 구독·사후 동기 검사, `HandleDefenderChanged` 두 시너지 부여)
- 작성 시점: 2026-05-17
- 불일치 건수: 0 개
- 전체 통과: Y
- 테스트 실행 명령: UnityMCP `run_tests` (mode=EditMode, group_names=`Tests.Editor.SummonTraitSynergyIntegrationTests`)
- 테스트 위치: `Assets/Tests/Editor/SummonTraitSynergyIntegrationTests.cs` (namespace `Tests.Editor`)
- TC 개수: 21 (TC-001/002, TC-101, TC-201/202 매개변수화 3, TC-301/302/304~308, TC-401~405, TC-501/502)
  - 재진입 후 TC-303 제거 (MIS-R1 처리).
- TC 결과: 21/21 통과 (재진입 후 재실행 기준)

## 불일치 항목

(최초 작성 시: 없음)

### 재진입 — 2026-05-17 (impl 핫픽스 `2e5eb39` 사후 검증)

impl 이 플레이 모드 NRE 해소 핫픽스를 커밋 (`SynergyManager.HandleSummonTraitsDistributed` 종료부의 `OnSynergyRecalculatedEventDto` 발행 루프 제거). main 의 사후 검증 요청에 따라 본 클래스 22 개를 재실행한 결과 TC-303 단일 실패 발생.

#### MIS-R1: HandleSummonTraitsDistributed 의 OnSynergyRecalculatedEventDto 미발행

- 관련 요구사항 (UR/SR DoD): 직접 매핑 없음 (이전 매핑 DoD-S5·S6·S7 은 부정확). UR/SR DoD 의 어디에도 "재계산 이벤트가 trait 별로 발행되어야 한다" 는 직접 명세가 없으며, DD §C-6 변경 2 의 *구현 메커니즘* 표현("활성화 등록 후 OnSynergyRecalculatedEventDto 를 활성화 항목별로 발행하여 UI 갱신 유도") 에서 도출된 검증이었다.
- 관련 TC: TC-303 (제거)
- 기대 동작 (테스트 기준): SummonTrait 등록 시 unique trait 수만큼 `OnSynergyRecalculatedEventDto` 발행.
- 실제 동작 (핫픽스 후): 발행 루프 제거. SynergyListPanel.Start 가 `_synergyActivations` 자체 폴링으로 SummonTrait 활성화를 바인딩 (executionOrder 100 으로 SynergyManager.Start 이후 실행).
- 테스트 코드: `Assets/Tests/Editor/SummonTraitSynergyIntegrationTests.cs:444` (제거 전)

**분류**: ② 테스트 오류
**분류 사유**: TC-303 의 검증 기준이 UR/SR DoD 의 black-box 요구가 아니라 DD 의 구현 메커니즘 표현에서 도출된 회귀였음. DoD-S5(인디케이터 표시 동등) / DoD-S6(상세보기 동등) / DoD-S7(집계 통합) 의 black-box 충족 조건은 `SynergyActivations` · `SynergyMembers` 단일 사전이 SummonTrait 합산 형태로 채워지는 것이며, 이는 TC-301 / TC-302 / TC-304 / TC-305 가 이미 커버한다. UI 갱신 보장은 핫픽스에서 명시한 SynergyListPanel.Start 폴링 메커니즘으로 동일 효과를 달성. 핫픽스 자체는 NRE 회피의 합리적 변경 + 사유 주석 명확.

**테스트 수정** (테스트 멤버 기록): TC-303 `Test_TC303_HandleDistributed_Publishes_Recalculated_Event_Per_Trait` 메서드 제거 (인라인 안내 주석 추가). 명세서 §DoD ↔ TC 매핑·§방법론별 도출 의 TC-303 항목 strike-through 처리. 명세서 §미커버 항목에 "재계산 이벤트 발행 (초기 통합 시점)" 항목 추가하여 핫픽스 결정 사유와 대체 메커니즘(SynergyListPanel.Start 폴링) 을 명시. 재실행 결과 21/21 통과.

## 본 구현 단위 범위 외 회귀 / 사전 실패

본 단계 테스트는 본 클래스만 필터링하여 실행했다 (group_names). 전체 EditMode 실행 결과는 trait-distribution 사이클과 동일 baseline 25 건 사전 실패가 보고서 capping 한도 내에 남아 있으며 — `ArcanistSynergyController*` / `SynergyController*` / `SynergyDetail*` UI 영역에 위치하고 본 구현 단위의 코드 경로와 직접 결합 없음. 별도 백로그에서 다뤄야 한다 — main 추적 메모에 이미 명시.

## 해결 기록

- 최초 실행: 2026-05-17 — 22 건 전체 통과. 첫 실행에서 모든 케이스 통과 (컴파일 후 force refresh 1 회 필요 — Unity 가 새 파일을 즉시 등록 안 함).
- 종료: 2026-05-17 — 본 구현 단위에 불일치 0 건, 전체 통과. 스테이지 7(불일치 해소) 무동작 / 스킵.
- 재진입: 2026-05-17 — impl 핫픽스 `2e5eb39` 사후 main 의 검증 요청. 22 건 재실행 → 1 건 실패(TC-303).
- 분류 완료: 2026-05-17 — ② 테스트 오류 1 건 (MIS-R1).
- 테스트 멤버 자체 처리: 2026-05-17 — TC-303 제거, 명세서·보고서 갱신.
- 재실행 결과: 2026-05-17 — 21/21 통과 (재진입 후 클린 상태).
- 종료(재진입): 2026-05-17 — 본 구현 단위 불일치 0 건 복귀.

## 명세 메모

- DoD-S5(인디케이터 표시 동등) / DoD-S6(상세보기 동등): UI 자체 코드(`SynergyListPanel` / `SynergyIndicator` / `SynergyDetailPanel`) 는 본 단계에서 미변경. UI 는 시너지 종류 분기 없이 `SynergyManager.SynergyActivations` · `SynergyMembers` 단일 사전을 소비하므로, SummonTrait 가 그 사전에 합산 형태로 들어가는 것이 표시·노출 충분 조건이다. 본 명세는 *사전 합산* 을 black-box 검증으로 다뤘다 (TC-301~305). UI 자체의 시각 표시·클릭 흐름 검증은 본 단위 테스트 범위 밖 — 통합·수동 QA 영역.
- DoD-S7(집계 통합): `HandleSummonTraitsDistributed` 의 활성화 등록·역참조 인덱스 합산·재계산 이벤트 발행 (TC-301~308), `HandleDefenderChanged(Spawn)` 의 두 시너지 부여(TC-401~405), Factory routing(TC-001/002), NoOp SSE 생성(TC-101) 으로 커버.
- OQ-9 타이밍 경합 보정: TC-501 (Store 비어 있는 상태에서 Start) / TC-502 (Store 데이터 있는 상태에서 Start — 사후 동기 검사) 두 케이스로 검증.
