# TACD-253 trait-store 요구사항 불일치 보고서

## 개요

- 구현 단위: `trait-store` (SR 그룹 B / DoD-S3·DoD-S4)
- 대응 클래스: `Assets/Scenes/Battle/Feature/SummonTrait/SummonTraitStore.cs`
- 작성 시점: 2026-05-17
- 불일치 건수: 0 개
- 전체 통과: Y
- 테스트 실행 명령: UnityMCP `run_tests` (mode=EditMode, assembly_names=`Assembly-CSharp-Editor`)
- 테스트 위치: `Assets/Tests/Editor/SummonTraitStoreTests.cs` (namespace `Tests.Editor`)
- TC 개수: 14 (TC-001 ~ TC-008, TC-010 ~ TC-014)
- TC 결과: 14/14 통과

## 불일치 항목

(없음 — 본 구현 단위 범위의 모든 테스트가 기대 동작과 일치)

## 본 구현 단위 범위 외 회귀 / 사전 실패

본 단계에서 `Assembly-CSharp-Editor` 전체 EditMode 테스트(332 개) 를 실행했다. 본 구현 단위 외에서 25 건의 실패가 보고서 capping 한도 내에서 확인됐으나 **이는 본 단계 시작 시점부터 동일하게 실패하던 사전 baseline 실패** 이며, 본 단계의 변경(테스트 코드 1 개 추가, 운영 코드 무수정) 으로 깨진 회귀가 아니다. SR 그룹·구현 단위가 다르므로 본 구현 단위의 MIS 항목으로 기록하지 않으며, 별도 백로그에서 다뤄야 한다.

확인된 사전 실패 영역(요약):
- `ArcanistSynergyControllerTests` (2 건) — 시너지 효과 적용 수치 불일치.
- `SynergyControllerTests` (3 건) — Defender 입출 시 SSE 재적용 수치 불일치.
- `SynergyDetailEffectSectionTests` (5 건) — UI Toolkit tier-list 자식 개수·USS 클래스 불일치, IndexOutOfRange.
- `SynergyDetailMemberListTests` (6 건) — Defender 활성 상태 USS 클래스 불일치, IndexOutOfRange.
- `SynergyDetailPanelLifecycleTests` (9 건) — UIDocument 미부착 Unhandled LogError, hidden USS 클래스 정합성.

> 본 구현 단위의 변경 파일은 운영 코드: 없음 (impl 이 신규 추가한 `SummonTraitStore.cs` 1 건은 본 단계 시작 시점에 이미 존재), 테스트 코드: `Assets/Tests/Editor/SummonTraitStoreTests.cs` 1 건 신규. 위 사전 실패들은 본 구현 단위의 코드 경로(`Scenes.Battle.Feature.SummonTrait`, `Common.Scripts.GlobalEventBus`, `Common.Scripts.SceneSingleton`, `Common.Scripts.SerializableDictionary`) 와 무관한 영역(`Synergy` 시너지 효과·UI Toolkit 패널) 에 위치한다.

## 해결 기록

- 최초 실행: 2026-05-17 — 14 건 중 2 건 실패 (TC-010·TC-011, "OnBattleWin/Lose 자동 Clear 미발동").
- 테스트 자체 진단(테스트 멤버 자체 수정): 2026-05-17 — 진단 테스트로 본 프로젝트 EditMode 환경에서 `AddComponent` / `DestroyImmediate` 가 Unity MonoBehaviour 라이프사이클(`Awake`/`OnEnable`/`OnDisable`) 을 자동 발화하지 않음을 확인. SetUp/TearDown 에서 reflection 으로 `OnEnable`/`OnDisable` 을 명시 호출하도록 테스트 인프라 수정. 운영 코드는 미변경.
- 재실행: 2026-05-17 — trait-store TC 14/14 통과.
- 종료: 2026-05-17 — 본 구현 단위에 불일치 0 건, 전체 통과.
